node_modules
dist
bazel-*
*.tsbuildinfo

# Zoom test tooling (puppeteer, installed on demand)
test-tools/
test-output/

# EchoSurface → Resonant Lab adapter — simulator run artifacts are
# reproducible from `configs/generated/*.yaml`, so don't track them.
# Anchored to repo root so the committed `public/generated/runs/` Pages
# mirror stays tracked (see scripts/build_echo_pages.py).
/runs/
scripts/__pycache__/
